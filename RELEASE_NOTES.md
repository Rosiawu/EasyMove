# EasyMove 0.6.19

## 0.6.19 修复

- 标题栏版本号不再写死，改为读取当前安装包的真实版本；本版会正确显示 0.6.19。
- 修复在顶部地址输入框按 Return 时，同时误触发 macOS“重命名”快捷键的问题；输入框聚焦期间文件操作快捷键不会抢占地址提交。
- 将文件或文件夹路径粘贴到顶部输入框、或任意窗格地址栏后立即直达：文件夹直接打开，文件打开所在文件夹并选中、高亮该文件。
- 路径直达同时支持绝对路径、带引号路径和 `file://` 地址；普通文字仍作为全局搜索关键词处理。
- 顶部提示改为“搜索当前窗格，或粘贴路径直达…”，不改变标题栏高度、字体、配色与原有朦胧水彩风格。

---

# EasyMove 0.6.18

## 0.6.18 修复

- 修复 SD 卡、U 盘及其他百度网盘沙盒外路径虽然进入上传队列、但随后显示“文件打开失败”的问题。
- 上传前把所选项目安全交接到百度网盘 App Group 缓存；文件保留原名称，文件夹会核对完整目录结构与各文件大小后再提交。
- 百度网盘确认 `error_code=0` 后自动删除对应临时批次；失败批次不会被立即误删，并会在 7 天后清理。
- 仍不申请“辅助功能”或“完全磁盘访问”权限，不使用 Finder、System Events 或模拟鼠标键盘。
- 使用百度网盘无法直接读取的 `/tmp` 路径完成真实上传验证：上传历史为 `error_code=0`，81 B 文件内容完整，成功后临时批次已自动删除。

---

# EasyMove 0.6.17

## 0.6.17 修复

- 撤回 0.6.16 无法完成上传的 Finder 二次确认方案，恢复真正的一键“上传到百度网盘”。
- 新实现直接连接百度网盘 8.2.1 自己的本机 `upload_file` 队列；文件、文件夹和多选批次无需打开 Finder 即可提交。
- 完全不申请“辅助功能”权限，不使用 System Events、界面辅助 API 或模拟鼠标键盘。
- 状态提示改为“已交给百度网盘上传队列”，避免把排队成功误报成云端传输已经完成。
- 使用“下载”目录中的独立 55 B 测试文件完成真实上传验证：百度上传历史记录 `error_code=0`，文件大小与源文件一致。

---

# EasyMove 0.6.16

## 0.6.16 修复

- 百度网盘入口彻底取消“辅助功能”权限，不再通过 System Events、界面辅助 API 或模拟鼠标操作 Finder 菜单。
- 右键“在 Finder 中上传到百度网盘…”会使用 macOS 公开接口在 Finder 中精准选中项目；用户随后通过百度网盘自己的 Finder 扩展确认上传。
- 状态提示明确显示“已在 Finder 中选中”，不会在用户确认之前误报“已经上传”。
- 百度网盘只提供 Finder 扩展、没有公开第三方本地上传接口，因此隐私优先模式会比自动化方案多一次 Finder 右键确认，但无需授予 EasyMove 控制整台电脑的权限。
- 移除辅助功能菜单模拟组件和实验性本地签名证书，打包继续使用普通临时签名；更新版本不再要求用户重复授权辅助功能。

---

# EasyMove 0.6.15

## 0.6.15 修复

- 修复 SD 卡或其他位置的照片点击右键“显示简介”后，Finder 虽已创建简介窗口、但窗口停留在 EasyMove 后方而像是没有反应的问题。
- 打开简介时会明确激活 Finder，并在创建简介窗口后再次置前；EasyMove 全屏或覆盖 Finder 时也能马上看到结果。
- 窗格内发生新的点击时会先取消上一次尚未触发的延迟预览，避免切换视图后旧项目突然弹出预览并遮挡操作。
- 使用当前 `SD_Card` 中的真实 JPG 路径验证 Finder 原生简介窗口和前台应用状态，并新增脚本命令回归测试。

---

# EasyMove 0.6.14

## 0.6.14 修复

- 修复应用启动后插入 SD 卡、U 盘或移动硬盘时，左侧磁盘列表不刷新的问题；磁盘插拔和窗口重新获得焦点都会自动重新枚举。
- macOS 现在读取系统真实磁盘属性，可移动磁盘优先显示；只读介质明确标注“只读”，避免到粘贴时才发现无法写入。
- 挂载的 EasyMove 安装 DMG 等磁盘镜像不再混进真实磁盘列表，侧栏更容易直接找到 SD 卡。
- 新增磁盘属性解析单测，以及真实 Electron 热插拔回归：测试磁盘插入后必须出现、移除后必须消失。

---

# EasyMove 0.6.13

## 0.6.13 调整

- 单击选中后的预览等待时间由 3 秒缩短为 2 秒，其余取消条件和视觉样式保持不变。
- 真实 Electron 时序测试同步调整：仅悬停超过 2 秒必须保持隐藏，单击后 1.7 秒仍隐藏，满 2 秒后才显示。

---

# EasyMove 0.6.12

## 0.6.12 调整

- 预览改为单击选中后延迟 3 秒显示；鼠标仅仅经过项目时不再自动弹出预览。
- 单击后的 3 秒内移开、滚动、右键或执行复制、剪切、粘贴，仍会立即取消预览。
- 新增真实 Electron 时序回归：仅悬停超过 3 秒必须保持隐藏，单击后 2.7 秒仍隐藏，满 3 秒后才显示。

---

# EasyMove 0.6.11

## 0.6.11 调整

- 文件与文件夹的悬停预览延迟由 0.4 秒调整为 4.2 秒，避免用户准备右键、复制、剪切或粘贴时过早遮挡界面。
- 鼠标在 4 秒内移开、滚动、右键或开始文件操作时，预览计时仍会立即取消。
- 新增真实 Electron 时序回归：悬停 3.9 秒时预览必须保持隐藏，超过 4.2 秒后才允许显示。

---

# EasyMove 0.6.10

## 0.6.10 修复

- 文件夹复制改为隐藏暂存、全目录校验、原子提交：中间状态不再冒充已经完成的副本。
- 每次复制都会固定源目录快照，并逐文件核对大小与 SHA-256 内容哈希；源内容在复制中变化或副本内容不一致时明确失败，不显示错误副本。
- 断点续传适配隐藏暂存目录，重启恢复后仍须通过完整校验才能生成正式文件或文件夹。
- 新增同一“归档”文件夹连续复制三次的自动与真实 Electron 回归，三份副本必须目录结构和文件内容逐字节一致。

---

# EasyMove 0.6.9

## 0.6.9 修复

- 右键“拷贝为路径名称”改为含义更明确的“拷贝完整路径”，与“重新命名”使用完全独立的命令通道。
- 复制完成后明确提示“已拷贝完整路径”，单选复制绝对路径，多选按行复制多条绝对路径。
- 新增回归断言：路径复制结果必须等于当前选中文件的完整路径，并且重命名窗口必须保持关闭。

---

# EasyMove 0.6.8

## 0.6.8 修复

- “拷贝为路径名称”得到的完整文件路径现在可以直接粘贴到顶部搜索/路径框并回车定位。
- 输入文件夹路径时直接进入该文件夹；输入图片或其他文件路径时打开父文件夹，精确选中文件、滚动到可见位置并短暂高亮。
- 隐藏文件路径也可以直达：必要时会为当前窗格自动显示隐藏项目后再定位。
- 新增真实 Electron 回归，验证完整图片路径必须定位到唯一对应文件，不能再误调用目录列表接口。

---

# EasyMove 0.6.7

## 0.6.7 修复

- 修复点击“全局搜索”后，关键词偶尔仍由旧的路径输入框处理，导致“灯下白”被解析成不存在的磁盘路径 `/灯下白`，搜索失败的问题。
- 普通窗格地址栏输入不带绝对路径标记的文字时，自动切换到全局搜索；只有 `/Users/...`、`~/...` 或 Windows 盘符路径才按文件路径打开。
- 全局搜索入口在窗格重绘前后都会重新聚焦可编辑搜索框，避免焦点停留在旧路径栏。
- 文件夹不存在时不再显示 Electron 的英文远程调用错误，改为清晰的中文提示。
- 新增真实 Electron 回归，覆盖在普通路径栏输入“灯下白”并回车，必须进入全局搜索且不得触发 `fs:list` 路径错误。

---

# EasyMove 0.6.6

## 0.6.6 修复

- 修复分栏视图把所有文件夹的目录占位值 `0` 错当成“已计算大小”，导致大量非空文件夹显示 `0 B` 的系统性问题。
- 目录接口不再为文件夹返回伪造的零字节；只有递归测量完成且确实为空的文件夹才显示 `0 B`，尚未完成时显示“计算中…”或“—”。
- 分栏展开后会分批计算其中全部文件夹的真实逻辑大小，并正确映射文件夹封面、内容摘要和错误状态。
- 普通目录从“只计算前 16 个文件夹”升级为后台分批计算全部文件夹；鼠标悬停、画廊或详情预览会优先核对当前文件夹。
- 顶部选择汇总与窗格底部统计现在同时识别主栏和展开栏项目，空文件夹也会准确显示 `0 B`，不会退回路径文字。
- 新增真实 Electron 回归：非空嵌套文件夹必须显示精确大小与内容摘要，只有真实空目录可显示 `0 B`，悬停卡片不得再出现伪零值。

---

# EasyMove 0.6.5

## 0.6.5 修复

- “复制”与“剪切”不再只写入 EasyMove 内部状态：新增原生 macOS Pasteboard 文件组件，同时写入标准文件 URL 与兼容文件列表类型。
- 从沉浸式照片预览、普通文件列表、图标、分栏和画廊视图复制后，可直接切换到微信或 Finder 使用 `⌘V` 粘贴真实文件。
- EasyMove 内部粘贴逻辑继续保留，因此同一次复制既能跨窗格整理，也能发送到其他应用。
- 新增独立进程系统剪贴板测试：复制后由 EasyMove 之外的进程读取并核对真实照片路径；测试前后的用户剪贴板内容会自动保存和恢复。
- 复制反馈明确显示“也可直接粘贴到微信或 Finder”；系统组件异常时不再误报跨应用复制成功。

---

# EasyMove 0.6.4

## 0.6.4 修复

- 沉浸式照片预览重新接入当前照片的 macOS 原生右键菜单，复制、剪切、重命名、制作副本、压缩、分享、显示简介、Finder 定位、百度网盘与废纸篓等功能全部恢复。
- 右键操作始终指向当前正在预览的照片，不会误作用于背景窗格或之前选中的文件。
- `⌘C` / `⌘X` 与 Windows `Ctrl+C` / `Ctrl+X` 在沉浸预览中继续有效；复制、剪切及百度网盘反馈会显示在预览层之上。
- 重命名、删除、制作副本或压缩前自动退出沉浸预览，使后续对话框和文件定位保持可见。

---

# EasyMove 0.6.3

## 0.6.3 修复

- 照片预览改为应用窗口内全屏沉浸模式，预览时完全隐藏标题栏、侧边栏、工具栏、文件窗格和传输栏。
- 预览背景延续当前主题的淡蓝水彩气质，文件名、序号、方向提示与关闭按钮改为轻量悬浮元素。
- 照片根据窗口尺寸尽可能放大并保持原始比例；方向键切换、空格和 Esc 关闭逻辑保持不变。
- 新增真实 Electron 覆盖范围断言，确保预览层完整覆盖 EasyMove 窗口，而不是退化成窗格内弹窗。

---

# EasyMove 0.6.2

## 0.6.2 修复

- 照片改用 EasyMove 内置大图预览：选中照片后按空格即可打开，按空格或 Esc 关闭。
- 大图预览支持上、下、左、右四个方向键，并按照图标网格中的实际位置切换照片；底部会显示当前序号和照片总数。
- 切换照片时同步更新窗格选择并自动滚动到可见位置，不会丢失当前文件夹或改变窗格布局。
- 文件夹、PDF、视频及其他文档仍使用 macOS 系统 Quick Look，保留原生预览能力。
- 修复后台生成缩略图时可能提前替换点击目标、干扰鼠标选择的问题。

---

# EasyMove 0.6.1

## 0.6.1 修复

- 左侧进入“全局搜索”后，窗格地址栏会变为可输入的关键词搜索框并自动聚焦；输入中文后按 Enter 即可搜索文件名与内容。
- 顶部搜索框与窗格搜索框双向同步，切换窗格或选择搜索结果时不会清空关键词。
- 第四种视图明确标示为“画廊预览”：文件显示大图或文档内容，文件夹显示封面或内部项目摘要，下方保留横向胶片条。
- 所选画廊项目会立即生成预览，不再依赖它是否恰好进入后台首屏预热范围。

---

# EasyMove 0.6.0

这次升级补齐了跨目录整理、全局查找和中断恢复，同时完整保留原有浪漫、朦胧、精致的水彩界面。

## 整理与工作区

- 新增“临时文件篮”：只收集引用，不移动或复制原文件，可从右键菜单加入、移除或一键清空。
- 新增工作区：保存并恢复窗格数量、路径、视图、排序、隐藏文件和分栏位置。
- 移除“制作替身”，保留更常用的 Finder 原生菜单能力。
- 修复异步缩略图或索引重绘期间，侧边栏偶发丢失点击的问题。

## 搜索、重命名与解压

- 新增独立本机 SQLite/FTS 内容索引，支持中文连续文字、文件名和常见文档内容搜索。
- 默认索引桌面、文稿和下载；可增减索引范围、暂停、继续、重建和清空，所有索引数据仅保留在本机。
- 新增批量重命名与实时预览，支持前缀、后缀、替换、编号、日期及 `⌃⌘R` / `Ctrl+Shift+R`。
- 新增 ZIP、TAR、TAR.GZ、TGZ、GZ 安全解压，并在落盘前拦截路径穿越与符号链接。

## 可恢复传输

- 传输队列持久化保存，可展开查看未完成、等待磁盘和最近完成的任务。
- 文件复制写入独立分片并记录断点；异常退出后重新打开 EasyMove 可直接继续。
- 跨磁盘移动先递归核对大小与 SHA-256，验证完成后才删除源文件。

## 验证

- 47 项自动测试全部通过。
- 原有 Electron 交互回归连续 5 轮通过，覆盖 Delete 删除、复制/剪切/粘贴、粘贴定位、稳定窗格、原生右键、Quick Look、撤销、冲突与失败重试。
- v0.6.0 Electron 专项测试通过，覆盖中文内容索引、临时文件篮、工作区、批量重命名、安全解压，以及强制退出后的断点续传和 SHA-256 完整性校验。

---

# EasyMove 0.5.4

## 0.5.4 修复

- 百度网盘上传改为调用 Finder 已验证的原生“上传至百度网盘”，不再直接写百度私有偏好文件，也不会误报上传成功。
- 自动检测并启用百度网盘 Finder 扩展；首次使用时清楚提示授予 EasyMove 辅助功能权限。
- 多选项目按所在文件夹分组交给 Finder，上传完成后焦点回到 EasyMove，文件窗格和界面风格保持不变。

---

# EasyMove 0.5.3

## 0.5.3 修复

- “上传到百度网盘”不再只是打开客户端，而是连接百度网盘 Finder 扩展的真实上传接口；文件、文件夹和多选项目会直接进入“我的网盘”上传队列。
- 上传成功投递后显示准确的项目数量和目标位置；未安装、未初始化或连接失败时显示对应原因。
- 页脚署名改为清晰可见的深紫灰色，字号 9px、字重 500，保持原有栏高和精致气质。

## 0.5.2 修复

- 修复 macOS 偶发出现 EasyMove 进程仍在、窗口却已消失，导致再次双击应用没有反应的问题。
- 双击应用、点击 Dock 图标或再次启动时，会重新创建缺失窗口；已有窗口则恢复、显示并置于前台。
- 修复跳过打包签名后遗留的无效应用签名，0.5.2 安装包会生成完整的本机临时签名，避免 Finder 因包体校验失败而拒绝双击启动。
- 保持原有浪漫朦胧界面、最近访问、百度网盘入口及纤细署名不变。

- 左侧新增 Finder 式“最近访问”，在当前窗格集中显示近期使用的文件与文件夹。
- 文件和文件夹原生右键菜单新增“上传到百度网盘”，由已安装的百度网盘客户端接管。
- 底部状态栏常驻极细灰紫色署名“Designed with care by Rosiawu”，不增加栏位高度。

---

# EasyMove 0.5.0

这是一次以文件操作可靠性和键盘效率为核心的升级，并完整保留原有浪漫、朦胧、精致的水彩界面。

## 文件操作与撤销

- 新增持久化操作历史，支持使用 `⌘Z` / `Ctrl+Z` 撤销复制、移动、重命名、新建文件夹和 macOS 废纸篓操作。
- 重名传输提供替换、保留双方、跳过与取消，可将决定应用到后续冲突。
- 替换前保存可恢复副本；失败传输可从操作历史中直接重试。
- 操作历史和冲突选择使用悬浮层，不改变任何窗格尺寸。

## 键盘效率

- 列表、图标、分栏和画廊视图支持方向键导航与自动滚动。
- `Shift + 方向键` 扩展或收缩连续选择。
- macOS 使用 `⌘↓` 打开项目、`⌘↑` 返回上级；Windows 使用 `Enter` 和 `Alt+↑`。
- Quick Look 打开后可用方向键连续切换预览项目，再按空格关闭当前预览。
- macOS 使用 `⌘⇧.`、Windows 使用 `Ctrl+H` 独立显示或隐藏当前窗格的隐藏文件。

## 验证

- 25 项自动测试全部通过。
- 已在真实 Electron 应用中验证键盘导航、连续 Quick Look、隐藏文件、操作历史、冲突处理、失败重试、原生右键菜单、删除和窗格稳定性。

---

# EasyMove 0.4.1

这是一次补齐 Finder 式空格预览，并继续完善 macOS 原生交互的测试版。

## 空格 Quick Look

- 在任意窗格内选中文件或文件夹后，单按空格即可打开 macOS 原生 Quick Look。
- 再按一次空格关闭当前 Quick Look；切换选择后按空格会直接预览新项目。
- 搜索框和重命名输入框聚焦时，空格仍用于正常输入，不会误触发预览。

## macOS 原生右键菜单

- 完全移除网页式右键浮层，改用 Electron 的 macOS 原生 `Menu`。
- 文件与文件夹支持打开、选择应用打开、Quick Look、剪切、复制、粘贴、重新命名、制作副本、制作 Finder 替身、压缩和移入废纸篓。
- 接入 macOS 原生“共享”与“服务”菜单，并提供显示简介、拷贝路径和在 Finder 中显示。
- 窗格空白处使用原生菜单，支持新建文件夹、粘贴、全选、显示当前文件夹简介及系统服务。
- Finder 私有的第三方“快速操作”和完整标签菜单不属于公开 Electron API，无法直接复制；系统公开的服务与共享扩展会原生显示。

## 界面调整

- macOS 左上角品牌图标、标题、版本号和花朵整体右移 32px，与红黄绿窗口按钮保持清晰间距；搜索框及右侧控件位置不变。

## 快捷键

- 新建文件夹、复制、剪切、粘贴、重命名和移入废纸篓/回收站全部接入统一快捷键处理。
- macOS 选中项目后单按 `⌫`（Delete）即可移入废纸篓，同时兼容 `⌘⌫`；Windows 单按 `Delete` 即可移入回收站，长按不会重复触发。
- 工具栏按钮悬停时显示当前系统对应的快捷键，并提供无障碍快捷键属性。
- 删除后仅刷新真正受影响的窗格，保留其他窗格和分栏的展开状态。

## 体积优化

- Electron 运行时仅保留简体中文与英文语言资源。
- DMG 改用更高压缩率的 BZIP2 格式，并启用最高打包压缩级别。
- 三张内置主题保持原尺寸，发布包改用 WebP，源代码中仍保留原始 PNG。

## 新增

- 鼠标悬停文件或文件夹整行约 400ms 后显示即时预览，不再要求精确悬停缩略图。
- 文件和文件夹右键菜单支持复制、剪切和粘贴。
- 粘贴完成后自动选中实际生成的文件，滚动到可见区域并短暂高亮。
- 复制与剪切支持 macOS Command 和 Windows Ctrl 快捷键。

## 修复

- 单击只选中项目，不再弹出遮挡其他窗格的大预览。
- 双击期间即使后台重绘造成视觉位置变化，仍锁定第一次点击的文件夹路径。
- 传输完成后只刷新真正变化的窗格，并保留其他窗格的分栏展开状态。

## 验证

- 已在真实 Electron 窗口中验证快捷键新建、重命名、复制、剪切、粘贴、移入废纸篓，以及粘贴后定位、分栏状态保留与双击路径锁定。
- 23 项自动测试全部通过。

## 安装提示

- macOS：当前 DMG 支持 Apple Silicon，尚未进行 Developer ID 签名与公证。首次启动如被拦截，请在 Finder 右键应用并选择“打开”。
- Windows：当前 EXE 支持 Windows 10/11 x64，尚未使用商业代码签名证书，SmartScreen 可能显示未知发布者。
