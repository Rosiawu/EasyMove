# EasyMove 0.1.3 发布说明

这是 EasyMove 的自定义主题版本。

## 本次更新

- 新增“我的”主题入口，可导入 PNG、JPEG 或 WebP 图片作为应用背景
- 自定义图片会复制到 EasyMove 的本地用户数据目录，不依赖原文件继续存在
- 自定义主题和当前主题选择会在应用重启后保留
- 可在三套内置主题与自定义主题之间随时切换
- 自定义图片限制为 50 MB，并在导入时验证图片是否可读取

## 安装包

- `EasyMove-0.1.3-mac-arm64.dmg`：macOS Apple Silicon（M 系列芯片）

## 首次启动

macOS 安装包未使用 Apple Developer ID 签名，也未经过 Apple 公证。打开 DMG 并将 EasyMove 拖入“应用程序”。首次启动若受阻，请在 Finder 中右键应用并选择“打开”。

## 验证摘要

- JavaScript 静态语法检查通过
- 双窗格、四窗格和文件选择回归通过
- 同盘拖动移动、修饰键拖动复制和外部文件拖入通过
- 三套内置主题切换通过
- 自定义背景真实图片解码、主题切换和重启持久化通过
- macOS DMG 镜像、挂载应用启动和核心工作流验证通过

## 校验文件

在终端中运行：

```bash
shasum -a 256 EasyMove-0.1.3-mac-arm64.dmg
```

输出应与 `SHA256SUMS-0.1.3.txt` 一致。
